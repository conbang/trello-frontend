import {User} from './user';
import {Board} from './board';

export interface TagUserBoard {
  id: number;
  appUser: User;
  board: Board;
}

// tslint:disable-next-line:class-name
export class Tag_user_board {
}
